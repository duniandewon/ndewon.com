import React from 'react';
import Header from '../components/Header';
import Contact from '../components/Contact';

const NotFound = () => (
  <React.Fragment>
    <Header headerSubTitle='Have a project in mind?' headerTitle="Come and say hi or just drop a line" />
    <Contact />
  </React.Fragment>
);

export default NotFound;
